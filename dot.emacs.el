;; -*- mode: emacs-lisp -*-
;;
;; Emacs config file (dot.emacs.el)
;; This file is to be loaded automatically by emacs and should then be
;; renamed `~/.emacs.el'.

;; It does nothing but load all files from the git repository (which
;; should be named `~/.emacs.d'

(load-file "~/.emacs.d/emacs-config.el")